# Research Reports

This directory is for generated offline evaluation outputs only.

Examples:

- calibration reports
- shortlist validation reports
- offline ranker training reports
- stress-test outputs

Do not treat files here as runtime assets.
They should be generated locally as needed and are ignored by git by default.
