The runtime service does not require local datasets from this folder.

- `data/archive/` keeps historical annotation packs, evaluation artifacts, and raw research data.
- active research datasets now live under `research/data/`.
